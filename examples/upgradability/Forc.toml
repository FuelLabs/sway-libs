[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
license = "Apache-2.0"
name = "upgradability_examples"

[dependencies]
# TODO: use latest release that include update SRC-14 ABIs
standards = { git = "https://github.com/FuelLabs/sway-standards", branch = "master" }
sway_libs = { path = "../../libs" }
